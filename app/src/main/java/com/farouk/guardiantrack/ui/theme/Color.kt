package com.farouk.guardiantrack.ui.theme

import androidx.compose.ui.graphics.Color

// ============ NEON SENTINEL — Design System by Stitch ============
// Creative North Star: "The Digital Ghost"
// An interface that feels like a high-end security operations center

// ===== THE VOID — Background Hierarchy =====
val Void = Color(0xFF050E1D)           // Base: deepest layer
val VoidDim = Color(0xFF040B17)        // Even deeper for contrast
val SurfContainerLowest = Color(0xFF000000)
val SurfContainerLow = Color(0xFF091324)    // Layer 1: secondary areas
val SurfContainer = Color(0xFF0E1A2C)       // Layer 2: default containers
val SurfContainerHigh = Color(0xFF132034)   // Layer 3: elevated cards
val SurfContainerHighest = Color(0xFF18263C) // Layer 4: active/interactive
val SurfBright = Color(0xFF1E2C44)          // Hover/highlight state

// ===== PRIMARY — Neon Cyan (The Light Source) =====
val NeonCyan = Color(0xFF81ECFF)       // Primary: headings, key data, active states
val NeonCyanDim = Color(0xFF00D4EC)    // Icons, secondary accents
val NeonCyanBright = Color(0xFF00E3FD) // Container fills, CTAs
val NeonCyanGlow = Color(0x1481ECFF)   // 8% opacity glow effect

// ===== SECONDARY — Cloud Silver =====
val CloudSilver = Color(0xFFDAE3F8)
val CloudSilverDim = Color(0xFFCBD4EA)
val SecondaryContainer = Color(0xFF3E4758)

// ===== TERTIARY — Signal Blue =====
val SignalBlue = Color(0xFF70AAFF)
val SignalBlueBright = Color(0xFF599CF9)
val SignalBlueDim = Color(0xFF5C9FFC)

// ===== ERROR / DANGER — Warm Coral =====
val Coral = Color(0xFFFF716C)          // Alert state
val CoralDim = Color(0xFFD7383B)       // Pressed state
val CoralContainer = Color(0xFF9F0519) // Background
val CoralSoft = Color(0xFFFFA8A3)      // On container text

// ===== EMERALD — Safe/Active =====
val Emerald = Color(0xFF00E676)
val EmeraldDark = Color(0xFF00C853)
val EmeraldLight = Color(0xFF69F0AE)
val EmeraldSurface = Color(0xFF0D2E1B)

// ===== AMBER — Warning =====
val Amber = Color(0xFFFFB300)
val AmberLight = Color(0xFFFFCA28)
val AmberDark = Color(0xFFFF8F00)
val AmberSurface = Color(0xFF2E2500)

// ===== TEXT HIERARCHY =====
val TextPrimary = Color(0xFFDCE5FB)    // on-surface: primary text
val TextSecondary = Color(0xFFA2ABC0)  // on-surface-variant: labels, subtitles
val TextMuted = Color(0xFF6C7689)      // outline: timestamps, metadata
val TextDisabled = Color(0xFF3F485A)   // outline-variant: disabled

// ===== SURFACE TINTS =====
val SurfTint = Color(0xFF81ECFF)
val InverseSurface = Color(0xFFF9F9FF)
val InverseOnSurface = Color(0xFF4C5567)
val InversePrimary = Color(0xFF006976)

// ===== LIGHT THEME EQUIVALENTS =====
val LightBackground = Color(0xFFF5F7FA)
val LightSurface = Color(0xFFF5F7FA)
val LightCard = Color(0xFFFFFFFF)
val LightCardElevated = Color(0xFFF0F2F5)
val LightPrimary = Color(0xFF006976)
val LightOnPrimary = Color(0xFFFFFFFF)
val LightTextPrimary = Color(0xFF1F2933)
val LightTextSecondary = Color(0xFF52606D)

// ===== UTILITY =====
val White = Color(0xFFFFFFFF)
val PureBlack = Color(0xFF000000)